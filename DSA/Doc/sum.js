
var arr = [3, -6, 1, 12];

//Tính tổng các số trong mảng a
var sum = function(nums) {
   let initalSum = 0;
    for(index of nums){
        initalSum +=index;
    }
    return initalSum;
};

console.log("Tổng các số: ", sum(arr));
//Tìm phần tử âm lớn nhất trong mảng a. Xuất giá trị và chỉ số tại vị trí đó
var negativeNum = function(nums) {
    let initalSum = 0;
     for(index of nums){
         initalSum =index;
     }
     return initalSum;
 };
 
 console.log("Tổng các số: ", sum(arr));

//Tính tổng các số lẻ trong mảng từ mảng a

//Tìm số lớn nhất trong mảng a và xuất chỉ số tại đó
